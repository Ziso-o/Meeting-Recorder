"""설정 및 보안 프로파일.

STUB — Phase 1에서 구현한다. 지금은 인터페이스 자리표시자만 둔다.

보안등급 분기:
- profile="secure"   → 로컬 LLM(Ollama). B2G 민감 회의.
- profile="internal" → 클라우드 LLM(Gemini 무료티어). 내부 회의.
"""

# TODO(Phase 1): .env 로딩 + 보안 프로파일별 프로바이더 선택 로직 구현.
