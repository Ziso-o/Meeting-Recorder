# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# uv
uv.lock

# Secrets
# 사용자 결정: private 레포라 .env 커밋 허용(두 PC 동기화 목적).
# ⚠️ public 전환/협업자 추가 시 키 노출 주의. 그 외 *.env(prod.env 등)는 계속 무시.
*.env
!.env.example
!.env

# Models / 대용량 데이터
models/
*.bin
*.pt

# 오디오 / 전사 산출물
*.wav
*.m4a
*.mp3
audio/
outputs/
*.transcript.json
*.minutes.json
*.minutes.md

# 백업 / Vexa 로컬 클론(최상위 폴더만 — src/minutes/vexa 는 소스라 무시하면 안 됨)
*.bak
/vexa/

# IDE / OS
.vscode/
.idea/
.DS_Store

# n8n / Docker 로컬 데이터
docker/n8n_data/
docker/ollama_data/

# 24/7 watch 로그
logs/
